blueprint:
  name: Paralelo Virtual Zigbee (À Prova de Loop)
  description: >
    Sincroniza o estado ON/OFF entre dispositivos Zigbee e um grupo de luzes.
    Compatível com o Editor Visual do Home Assistant.
    Inclui debounce inteligente, modo seguro contra interrupções e filtro de eventos para evitar loop infinito.
  domain: automation
  input:
    grupo_zigbee:
      name: Grupo de Dispositivos Zigbee
      selector:
        entity:
          domain: light
    dispositivos_individuais:
      name: Dispositivos Individuais Zigbee
      selector:
        entity:
          domain:
            - light
            - switch
          multiple: true
    debounce_flag:
      name: Input Boolean para controle de debounce
      selector:
        entity:
          domain: input_boolean

trigger:
  - platform: state
    entity_id: !input dispositivos_individuais
    to: "on"
    id: ligado
  - platform: state
    entity_id: !input dispositivos_individuais
    to: "off"
    id: desligado

condition: []

action:
  - choose:
      - conditions:
          - condition: state
            entity_id: !input debounce_flag
            state: "off"
          - condition: template
            value_template: >
              {{ trigger.to_state.state != trigger.from_state.state }}
          - condition: trigger
            id: ligado
        sequence:
          - service: input_boolean.turn_on
            target:
              entity_id: !input debounce_flag
          - service: light.turn_on
            target:
              entity_id: !input grupo_zigbee
          - delay:
              milliseconds: 500

      - conditions:
          - condition: state
            entity_id: !input debounce_flag
            state: "off"
          - condition: template
            value_template: >
              {{ trigger.to_state.state != trigger.from_state.state }}
          - condition: trigger
            id: desligado
        sequence:
          - service: input_boolean.turn_on
            target:
              entity_id: !input debounce_flag
          - service: light.turn_off
            target:
              entity_id: !input grupo_zigbee
          - delay:
              milliseconds: 500

  # Desligamento garantido do debounce
  - service: input_boolean.turn_off
    target:
      entity_id: !input debounce_flag

mode: queued
max_exceeded: silent
