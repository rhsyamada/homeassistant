blueprint:
  name: Paralelo Virtual Zigbee (Debounce Inteligente)
  description: >
    Sincroniza o estado ON/OFF entre dispositivos Zigbee e um grupo de luzes.
    Compatível com o Editor Visual do Home Assistant.
    Usa debounce inteligente: primeiro evento dispara na hora, repetidos são bloqueados por 500ms.
  domain: automation
  input:
    grupo_zigbee:
      name: Grupo de Dispositivos Zigbee
      selector:
        entity:
          domain: light
    dispositivos_individuais:
      name: Dispositivos Individuais Zigbee
      selector:
        entity:
          domain:
            - light
            - switch
          multiple: true
    debounce_flag:
      name: Input Boolean para controle de debounce
      selector:
        entity:
          domain: input_boolean

trigger:
  - platform: state
    entity_id: !input dispositivos_individuais
    to: "on"
    id: ligado
  - platform: state
    entity_id: !input dispositivos_individuais
    to: "off"
    id: desligado

condition: []

action:
  - choose:
      - conditions:
          - condition: trigger
            id: ligado
          - condition: state
            entity_id: !input debounce_flag
            state: "off"
        sequence:
          - service: input_boolean.turn_on
            target:
              entity_id: !input debounce_flag
          - service: light.turn_on
            target:
              entity_id: !input grupo_zigbee
          - delay:
              milliseconds: 500
          - service: input_boolean.turn_off
            target:
              entity_id: !input debounce_flag

      - conditions:
          - condition: trigger
            id: desligado
          - condition: state
            entity_id: !input debounce_flag
            state: "off"
        sequence:
          - service: input_boolean.turn_on
            target:
              entity_id: !input debounce_flag
          - service: light.turn_off
            target:
              entity_id: !input grupo_zigbee
          - delay:
              milliseconds: 500
          - service: input_boolean.turn_off
            target:
              entity_id: !input debounce_flag

mode: restart
max_exceeded: silent
